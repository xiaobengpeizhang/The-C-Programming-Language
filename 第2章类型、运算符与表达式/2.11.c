/**下面这个循环语句打印一个数组的n个元素，每行打印10个元素，每列之间用一个空格隔开。每行用一个换行符结束（包括最后一行）**/

#include <stdio.h>

int main()
{
    int a[25];
    int i = 0;
    for (i = 0; i < 25; i++)
        a[i] = 10;
    for (i = 0; i < 25; i++)
        printf("%6d%c", a[i], ((i % 10 == 9) || i == 24) ? '\n' : ' ');
}
